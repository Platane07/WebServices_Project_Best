<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="#">BOUTIQUE</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="/">{{ 'default.navbar.accueil' | trans }}
					<span class="sr-only">(current)</span>
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href={{ path("contact") }}>
					{{ 'default.navbar.contact' | trans}}</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href={{ path("boutique") }}>
					{{ 'default.navbar.boutique' | trans}}</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href={{ path("panier") }}>
					{{ 'default.navbar.panier' | trans}} : {{ countPanier }} </a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href={{ path("usager_accueil") }}>
					{{ 'default.navbar.compte' | trans}}</a>
			</li>
			{% if is_granted('ROLE_ADMIN')  %}
				<li class="nav-item">
					<a class="nav-link" href={{ path("admin_accueil") }}>
						{{ 'default.navbar.admin' | trans}} </a>
				</li>
			{% endif %}
		</ul>
    <ul class="navbar-nav">
		{% if app.user %}
			<li class="nav-item">
				<a class="btn btn-primary" href={{ path("app_logout") }}>
					{{ 'default.navbar.deconnexion' | trans}}
				</a>
			</li>
		{%  endif %}
		<li class="nav-item dropdown">
			<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
				{{ 'default.navbar.devises' | trans}}
			</a>
			<div class="dropdown-menu" aria-labelledby="navbarDropdown">
				<a class="dropdown-item" href={{ path("set_devise", { 'devise': 'EUR'}) }}>EUR</a>
				<a class="dropdown-item" href={{ path("set_devise", { 'devise': 'CAD'}) }}>CAD</a>
				<a class="dropdown-item" href={{ path("set_devise", { 'devise': 'USD'}) }}>USD</a>
				<a class="dropdown-item" href={{ path("set_devise", { 'devise': 'GBP'}) }}>GBP</a>
				<a class="dropdown-item" href={{ path("set_devise", { 'devise': 'PHP'}) }}>PHP</a>
				<a class="dropdown-item" href={{ path("set_devise", { 'devise': 'IDR'}) }}>IDR</a>
			</div>
		</li>

    	<li class="nav-item dropdown">
			<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
				{{ 'default.navbar.langues' | trans}}
			</a>

			<div class="dropdown-menu" aria-labelledby="navbarDropdown">
        {% for locale in supported_locales|split('|') %}
          <a class="dropdown-item" href={{ path(route, params | merge({'_locale': locale })) }}>{{ locale }}</a>
        {% endfor %}
			</div>
		</li>
    </ul>
		<form class="form-inline my-2 my-lg-0" action={{ path("chercher") }} method="POST">
			<input class="form-control mr-sm-2" type="chercher" name="chercher" placeholder="Chercher un produit" aria-label="Search">
			<button class="btn btn-outline-success my-2 my-sm-0" type="submit">
				{{ 'default.navbar.chercher' | trans}}</button>
		</form>
		
	</div>
</nav>
